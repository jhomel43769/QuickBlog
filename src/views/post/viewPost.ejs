<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= post.title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen p-6">

  <div class="max-w-3xl mx-auto bg-white p-8 rounded-xl shadow-md">
    <h1 class="text-4xl font-bold mb-4"><%= post.title %></h1>

    <p class="text-gray-600 mb-6">
      <%= post.content %>
    </p>

    <div class="text-sm text-gray-500 space-y-1 mb-6">
      <p><strong>Categoría:</strong> <%= post.category || 'Sin categoría' %></p>
      <p><strong>Publicado:</strong> <%= post.published ? 'Sí' : 'No' %></p>
      <p><strong>Creado el:</strong> <%= post.createdAt.toLocaleDateString() %></p>
    </div>

    <div class="flex justify-between">
      <a href="/view/list" class="text-blue-600 hover:underline text-sm">← Volver al listado</a>
      <a href="/view/edit/<%= post._id %>" class="text-yellow-600 hover:underline text-sm">Editar</a>
    </div>
  </div>

</body>
</html>
